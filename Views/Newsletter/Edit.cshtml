@model BootstrapVillas.Models.NewsletterParticipant

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>NewsletterParticipant</legend>

        @Html.HiddenFor(model => model.NewsletterParticipantID)

        <div class="editor-label">
            @Html.LabelFor(model => model.NewsletterParticipantEmail)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NewsletterParticipantEmail)
            @Html.ValidationMessageFor(model => model.NewsletterParticipantEmail)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
