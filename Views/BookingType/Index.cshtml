@model IEnumerable<BootstrapVillas.Models.BookingType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Content/Admin/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookingType1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookingTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookingType1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookingTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookingTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookingTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookingTypeID })
        </td>
    </tr>
}

</table>
