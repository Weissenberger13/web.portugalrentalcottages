@model BootstrapVillas.Models.BookingType

@{
    ViewBag.Title = "Edit";
    Layout = "~/Content/Admin/_AdminLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>BookingType</legend>

        @Html.HiddenFor(model => model.BookingTypeID)

        <div class="editor-label">
            @Html.LabelFor(model => model.BookingType1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BookingType1)
            @Html.ValidationMessageFor(model => model.BookingType1)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.BookingTypeDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BookingTypeDescription)
            @Html.ValidationMessageFor(model => model.BookingTypeDescription)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
