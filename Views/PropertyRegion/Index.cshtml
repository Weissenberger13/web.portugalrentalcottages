@model IEnumerable<BootstrapVillas.Models.PropertyRegion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegionCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegionCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PropertyRegionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PropertyRegionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PropertyRegionID })
        </td>
    </tr>
}

</table>
