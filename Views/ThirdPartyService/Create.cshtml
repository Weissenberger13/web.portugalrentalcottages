@model BootstrapVillas.Models.ThirdPartyService

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ThirdPartyService</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ThirdPartyServiceName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ThirdPartyServiceName)
            @Html.ValidationMessageFor(model => model.ThirdPartyServiceName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
