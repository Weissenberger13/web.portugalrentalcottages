@model BootstrapVillas.Models.PropertyPricingSeasonalInstance

@{
    ViewBag.Title = "Create";
    
       AjaxOptions Options = new AjaxOptions
    {
        UpdateTargetId = "BottomEditorWindow",
        InsertionMode = InsertionMode.Replace,
        LoadingElementId = "spinner",
    
    };

}

<h2>Create</h2>

@using (Ajax.BeginForm("Create", "PropertyPricingSeasonalInstance", Options))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PropertyPricingSeasonalInstance</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.PropertyPricingSeasonID, "PropertyPricingSeason")
        </div>
        <div class="editor-field">
            @Html.DropDownList("PropertyPricingSeasonID", String.Empty)
            @Html.ValidationMessageFor(model => model.PropertyPricingSeasonID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PropertyID, "Property")
        </div>
        <div class="editor-field">
            @Html.DropDownList("PropertyID", String.Empty)
            @Html.ValidationMessageFor(model => model.PropertyID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Price)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Price)
            @Html.ValidationMessageFor(model => model.Price)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
